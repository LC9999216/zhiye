# =====================================================================
# 知辨 (Zhibian) MVP — Web 前端环境变量示例（apps/web）
# ---------------------------------------------------------------------
# 使用方法：复制本文件为 apps/web/.env.local 后按需修改。
# 真实 .env.local 已被仓库 .gitignore 忽略，不会被提交。
# =====================================================================

# 后端 FastAPI 服务基础地址（浏览器端调用 /api/health、/api/queries 等接口）
NEXT_PUBLIC_API_URL=http://localhost:8000
